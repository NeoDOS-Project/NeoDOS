{
  "$schema": "https://opencode.ai/config.json",
  "provider": {
      "llmstudio": {
      "npm": "@ai-sdk/openai-compatible",
      "name": "LM Studio (local)",
      "options": {
        "baseURL": "http://localhost:1234/v1",
        "apiKey": "lm-studio-api-key"
      },
      "models": {
        "google/gemma-4-e4b": { "name": "Gemma 4-e4b" }
      }
    }
  },
  "instructions": ["AGENTS.md"],
  "command": {
    "build": {
      "template": "Build the NeoDOS OS: compile bootloader + kernel and create the FAT32 disk image. Run from the neodos/ directory.",
      "description": "Build bootloader, kernel, and disk image"
    },
    "build-lsp": {
      "template": "Build the NeoDOS LSP server: `cd neodos-lsp && cargo build --release`. The binary will be at neodos-lsp/target/release/neodos-lsp.",
      "description": "Build the NeoDOS LSP server"
    },
    "run": {
      "template": "Launch NeoDOS in QEMU with OVMF UEFI firmware, serial output, and GDB server on :1234. Run from neodos/ after building.",
      "description": "Run NeoDOS in QEMU"
    },
    "debug": {
      "template": "Connect GDB to a running QEMU instance (port :1234) with kernel symbols loaded. Run from neodos/ using gdb -x .gdbinit",
      "description": "Debug with GDB"
    },
    "github": {
      "template": "Review uncommitted changes in the repo (master branch). Check that docs/ files (AGENTS.md, ARCHITECTURE.md, KERNEL.md, IMPROVEMENTS.md, CHANGELOG.md, NEM_SPEC.md, etc.) are up to date with any new features or changes. Build kernel + user binaries, then run auto_test.py. Commit and push to GitHub with descriptive message.",
      "description": "Release to GitHub"
    }
  },
  "lsp": {
    "neodos-lsp": {
      "command": ["./neodos-lsp/target/release/neodos-lsp"],
      "extensions": ["rs"],
      "disabled": false,
      "env": {
        "RUST_LOG": "neodos_lsp=info",
        "NEODOS_LSP_ROOT": "."
      }
    }
  },
  "mcp": {
    "neodos-mcp": {
      "type": "local",
      "command": ["bash", "scripts/mcp-server.sh"],
      "enabled": true
    }
  }
}
