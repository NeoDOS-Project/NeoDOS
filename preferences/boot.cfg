# NeoDOS Boot Configuration
# Benchmark and debug flags (default: 1 = enabled)
BENCHMARK_REPORT=0
AHCI_DEBUG=0
# Set NEOINIT=0 to skip NeoInit (PID 1) and boot directly into kernel shell for testing
NEOINIT=0