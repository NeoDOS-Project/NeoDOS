[package]
name = "kill"
version = "0.1.1"
edition = "2021"

[[bin]]
name = "kill"
path = "src/main.rs"
test = false

[dependencies]
libneodos = { path = "../../libneodos" }

[profile.release]
panic = "abort"
opt-level = 3
lto = true
debug = false

[profile.dev]
panic = "abort"
