[package]
name = "nem-rtc"
version = "0.1.0"
edition = "2021"

[lib]
name = "nem_rtc"
path = "src/lib.rs"
crate-type = ["lib"]

[profile.release]
panic = "abort"
opt-level = 3
lto = true
debug = false

[profile.dev]
panic = "abort"
