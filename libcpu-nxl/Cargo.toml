[package]
name = "libcpu-nxl"
version = "0.1.0"
edition = "2021"
description = "NeoDOS CPU information NXL — provides CPUID data to user-mode programs"

[profile.release]
panic = "abort"
opt-level = 3
lto = true
debug = false

[profile.dev]
panic = "abort"
