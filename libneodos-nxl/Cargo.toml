[package]
name = "libneodos-nxl"
version = "0.1.0"
edition = "2021"
description = "NeoDOS shared library (NXL) — compiled as standalone binary at 0x1e000000"

[profile.release]
panic = "abort"
opt-level = 3
lto = true
debug = false

[profile.dev]
panic = "abort"
