[package]
name = "libmath-dll"
version = "0.1.0"
edition = "2021"
description = "NeoDOS math library (DLL) — compiled as standalone binary at 0x1e040000"

[profile.release]
panic = "abort"
opt-level = 3
lto = true
debug = false

[profile.dev]
panic = "abort"
